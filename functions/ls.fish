##############################################################################
# Set some base options for the ls command.
function ls -d "Base ls command with colour added"
    /bin/ls -F --color $argv
end

### ls.fish ends here
